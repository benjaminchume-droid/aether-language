# Compiler fixtures

`.ae` files in this directory exercise compiler behavior and serve as readable language conformance inputs. Executable Rust regression tests live alongside the compiler crate until the dedicated test runner is implemented.
