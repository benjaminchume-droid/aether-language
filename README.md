# Aether Language

Aether (`.ae`) is the native programming language of the Aether computing ecosystem.

This repository contains the compiler, runtime, standard library, tooling, language services, and platform integrations.

The implementation is built as a complete system in dependency order, with Rust used for bootstrap infrastructure and Aether progressively becoming self-hosting.
