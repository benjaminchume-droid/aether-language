# Aether standard library

The standard library is the portable API surface of Aether. It will provide collections, text, I/O, filesystem, networking, serialization, cryptography, time, math, concurrency, async, processes, and system APIs without making application code depend directly on OS-specific implementations.
