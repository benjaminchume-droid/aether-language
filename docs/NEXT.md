# Next implementation boundary

Continue from the current front-end without replacing it. The next functional additions are richer types and declarations, assignment/mutation semantics, collections, structs and enums, matching, closures, modules/imports, and a real constraint-based type checker. After that, execution and memory semantics become the next dependency boundary.
